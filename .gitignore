.env
__pycache__/
*.pyc
.claude/
*.egg-info/
.venv/
venv/
CLAUDE.md
graphify-out/

# Frontend build artifacts
frontend/node_modules/
web-dist/
.agents/

# AI dev / agent + interview-prep artifacts — local only, never publish
PREP_STATUS.md
PRESENTATION.md
RESEARCH_OBSERVATIONS_SESSION2.md
# skills installer metadata
skills-lock.json

# internal build/decision log — local only, never publish
BUILD_NOTES.md

# presentation / slides — local only, never publish
*.pdf
